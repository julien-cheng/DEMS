import { IterativeObjectPipe } from './iterative-object.pipe';

describe('IterativeObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new IterativeObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
