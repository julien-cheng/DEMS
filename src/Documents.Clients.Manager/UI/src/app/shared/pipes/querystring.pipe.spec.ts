import { QuerystringPipe } from './querystring.pipe';

describe('QuerystringPipe', () => {
  it('create an instance', () => {
    const pipe = new QuerystringPipe();
    expect(pipe).toBeTruthy();
  });
});
