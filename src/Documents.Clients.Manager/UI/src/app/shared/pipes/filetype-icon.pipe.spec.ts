import { FiletypeIconPipe } from './filetype-icon.pipe';

describe('FiletypeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new FiletypeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
