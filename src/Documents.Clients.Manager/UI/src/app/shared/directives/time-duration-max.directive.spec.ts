import { TimeDurationMaxDirective } from './time-duration-max.directive';

describe('TimeDurationMaxDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeDurationMaxDirective();
    expect(directive).toBeTruthy();
  });
});
