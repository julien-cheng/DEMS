import { TimeStringValidatorDirective } from './time-string-validator.directive';

describe('TimeStringValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeStringValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
