import { TimeRangeMinDirective } from './time-range-min.directive';

describe('TimeRangeMinDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeRangeMinDirective();
    expect(directive).toBeTruthy();
  });
});
