import { GotopDirective } from './gotop.directive';

describe('GotopDirective', () => {
  it('should create an instance', () => {
    const directive = new GotopDirective();
    expect(directive).toBeTruthy();
  });
});
